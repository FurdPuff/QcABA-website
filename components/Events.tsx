const Events = () => {
    return(
        <main style={{textAlign: 'center', marginBottom: '150px'}}>
            <h3 className="text-xl">No Events at the moment</h3>
        </main>
    )
}

export default Events